# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables (never commit real secrets to a public repo)
.env
.env.*
!.env.example

# AWS SAM / CloudFormation
.aws-sam/
samconfig.toml
packaged.yaml
packaged.yml
env.json
*.env.json

# AWS credentials / config (should live in ~/.aws, never in the repo)
.aws/
**/aws-exports.js
*.pem
*.key

# AWS CDK
cdk.out/
**/cdk.out/
cdk.context.json
.cdk.staging/

# Common ignores
node_modules
.next/
.DS_Store

.kiro*

# Seed data: not committed (question source + raw figures stay local).
# The served figure copies live in public/figures/.
data/